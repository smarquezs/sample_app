<%= simple_form_for @user do |f| %>
	<%= f.input :name %>
	<%= f.input :email %>
	<%= f.input :password %>
	<%= f.input :password_confirmation, label: 'Confirmation' %>
	<%= f.button :submit, value: 'Create my account', class: 'btn btn-primary btn-large' %>
<% end -%>